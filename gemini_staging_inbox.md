# 🛸 Gemini Idea & Staging Inbox

Welcome to your unified Staging Inbox! Since we can't link our accounts directly, use this file to dump all your great ideas, code snippets, architectural prompts, and workflow requests generated by your web Gemini session.

Because this file lives natively inside your IDE, I (Antigravity) have direct, instantaneous access to it!

---

## 📥 How to use this:

1. Keep this document open as a tab in your IDE.
2. Whenever Web-Gemini generates a cool feature or snippet, paste it into the **Pending Ideas** section below.
3. Tell me in the chat: _"Hey, look at the inbox and implement the next feature."_
4. I will read the file, build the code natively into `Ignition OS`, and then check off or move the idea to the **Archived/Completed** section so you never lose track of what we've done!

---

## Pending Ideas (Paste Web-Gemini output here)

- [ ] /\*\*

* MODULE: SECURE (Security & Rates Registry)
* VERSION: v1.0.0
  \*/

const SecurityRegistry = {
roles: {
ADMIN: ["view_omni", "edit_margins", "manage_users", "approve_payroll"],
TECH: ["view_kosk", "scan_parts", "update_flux"],
CUSTOMER: ["view_port", "sign_estimates", "pay_invoice"]
},

laborRates: {
BASE: 165.00,
DIAGNOSTIC: 195.00,
MOBILE: 225.00,
MARKUP*ON_SUBLET: 20 // % markup if you outsource a job
}
};\_Paste your first idea here...*

- [ ] \_/\*\*

* DATA STRUCTURE: User Profile
  \*/
  const UserProfile = {
  id: "E-102",
  name: "Sarah Miller",
  pin: "1234",
  // Multiple permission keys assigned to one person
  permissions: ["FRONT_OFFICE", "INVENTORY", "LOGISTICS"]
  };

/\*\*

- LOGIC: The Access Gatekeeper
  \*/
  const hasAccess = (requiredPermission) => {
  const currentUser = DataBridge.get('current*user');
  return currentUser.permissions.includes(requiredPermission);
  };Paste your next idea here...*

* [ ] **FEATURE: Blended Labor Profitability Report**
      **Logic:** In `IgnitionOmni.js`, create a `LaborEfficiency` widget.
      **Calculation:** - Pull `total_flat_rate_hours` from the Work Order. - Pull `sum_of_active_tech_clocks` from the job's tech array. - **Efficiency %** = (Flat Rate / Actual Time) \* 100.
      **UI:** Highlight jobs in **RED** if Efficiency falls below 80% (indicating too many techs or slow work).

      - [ ] **CORE: The IgnitionCore Router**
      **Logic:** Create `IgnitionCore.js` to act as the main entry point.
      **Routing:**
      1. If `URL` contains `enroll`, mount `EnrollmentCatch`.
      2. If `current_user.permissions` includes `ADMIN`, mount `IgnitionOmni`.
      3. If `permissions` includes `TECH`, mount `IgnitionKosk`.
      4. If `permissions` includes `CUSTOMER`, mount `IgnitionPort`.
      **Security:** If no user exists, redirect to a "Secure Identity Required" splash screen.

      - [ ] **FEATURE: Fail-Safe DOT Form Integration**
      **Logic:** Update `IgnitionCompliance.js` to use "Strict Type Checking."
      - If `DataBridge.load('telematics')` returns null or is corrupted, do NOT crash.
      - **Fallback:** Display "TELEMATICS OFFLINE" and enable a manual mileage input field.
      - **Audit:** Tag the form with `data_source: "MANUAL_ENTRY"` so the owner knows the API was down during this inspection.
      **UI:** Ensure the 7-Point checklist buttons are "Large-Target" for greasy hands (min 60px height).

      - [ ] **SECURITY: The "Nuke" Option (Staff Revocation)**
      **Logic:** In the Staff Directory, the 'REVOKE' button must trigger a two-step confirmation.
      - **Step 1:** "Are you sure you want to revoke [Name]'s access?"
      - **Step 2:** Owner must type the word "REVOKE" to confirm.
      **Payload:** Once confirmed, send an `IDENTITY_WIPE` signal to the `DataBridge`. If that tech is currently clocked into a job, auto-clock them out first to preserve labor logs before deleting the ID.

      - [ ] **FEATURE: Smart Auto-Lock & Session Persistence**
      **Logic:** Create a `useSessionGuard` hook in `IgnitionCore.js`.
      - **Timer:** Set a `TIMEOUT_CONST = 300000` (5 mins).
      - **Policy Check:** Pull `shop_policy.autoLockEnabled` from DataBridge.
      - **Action:** If `autoLockEnabled` is TRUE, reset a timer on every touch/click. If timer hits zero, `DataBridge.save('current_user', null)` and redirect to Splash.
      - **Override:** If `autoLockEnabled` is FALSE, the timer is cleared and the session persists indefinitely.
      **UI:** Add a 'Stay Logged In' toggle to the **OMNI** Settings and a temporary 'Coffee Pot' icon in the **KOSK** header to show that the lock is currently disabled.

      /**

- HOOK: useSessionGuard
- DESC: Monitors activity and forces a lockout based on Shop Policy.
  \*/

const useSessionGuard = () => {
useEffect(() => {
const policy = DataBridge.load('shop_policy') || { autoLockEnabled: true };
if (!policy.autoLockEnabled) return; // Flexibility Mode: Do nothing.

    let timeout;

    const resetTimer = () => {
      clearTimeout(timeout);
      timeout = setTimeout(() => {
        console.warn("SESSION EXPIRED: Auto-locking for security.");
        DataBridge.save('current_user', null);
        window.location.reload(); // Force a clean slate
      }, 300000); // 5 Minutes
    };

    // Watch for greasy fingers on the screen
    window.addEventListener('mousemove', resetTimer);
    window.addEventListener('touchstart', resetTimer);
    window.addEventListener('click', resetTimer);

    resetTimer(); // Start the clock

    return () => {
      window.removeEventListener('mousemove', resetTimer);
      window.removeEventListener('touchstart', resetTimer);
      window.removeEventListener('click', resetTimer);
      clearTimeout(timeout);
    };

}, []);
};

- [ ] **FEATURE: Velocity Leaderboard (with Management Toggle)**
      **Logic:** In `IgnitionOmni.js`, create a `Leaderboard` component. - **Metrics:** Rank techs by (Flat Rate Hours / Actual Clocked Hours). - **Management Switch:** Add `shop_policy.showLeaderboard` (Boolean). - **Safety Check:** If a job has a 'RE-WORK' tag, exclude that tech's velocity from the board for 24 hours.
      **UI:** High-contrast ranking for KOSK. If `showLeaderboard` is FALSE, display "Shop Average Efficiency" instead of individual names.

- [ ] **FEATURE: Hardware Ledger & Tool Tracking**
      **Logic:** Update the `STOK` schema to include a `Hardware` table. - **Fields:** `id`, `description`, `owner_type` (SHOP | TECH), `status` (IN_BAY | ON_TRUCK | MISSING). - **Workflow:** When a tech punches out of a mobile unit in **HUB**, they must check off 'All Shop Tools Accountable'.
      **UI:** Create a 'Missing Tool' alert in OMNI. If a SHOP tool status is 'MISSING', highlight in **RED**.

      - [ ] **FEATURE: Hardware Investigation Workflow**
      **Logic:** Create an `InvestigationLog` in the `DataBridge`.
      - **Trigger:** When a `Hardware` item status moves to 'MISSING'.
      - **Manager View:** Create a 'Pending Investigations' list in OMNI.
      - **Form Fields:** Finding (Recovered | Broken | Lost), Location Found, and Resolution Note.
      **Action:** Upon submission, update the `Hardware` table status and append the finding to the `admin_audit_log` for end-of-month loss prevention reporting.
      **UI:** High-visibility 'Alert' badge on the OMNI Sidebar if investigations are pending.

      - [ ] **HARDENING: Hardware Chain of Custody**
      **Logic:** Update `Hardware` schema to include `last_assigned_tech` and `last_assigned_unit`.
      - **KOSK Sync:** When a tech scans a tool via the 'Scan Part / Bin' button, if the item is 'Hardware', update the `last_assigned_tech` to the `current_user.id`.
      - **HUB Sync:** When a truck is dispatched, any SHOP-owned hardware assigned to that truck's manifest must update its `last_assigned_unit`.
      **Investigation Link:** The `InvestigationLog` must automatically pull these two IDs to provide the Manager with a "Starting Point" for their search.

      - [ ] **ARCHITECTURE: Feature Maturity Toggles**
      **Logic:** In `DataBridge.shop_policy`, add `featureLevels: { hardware: 0, leaderboard: 0 }`.
      **UI Implementation:**
      - **KOSK:** Use a conditional wrapper: `{policy.featureLevels.hardware > 0 && <HardwareScanner />}`.
      - **OMNI:** If level is 1 (Passive), the 'Investigation' is a simple 'Dismiss' button. If level is 2 (Active), the 'Dismiss' button is replaced by the 'Mandatory Finding Report'.
      **Goal:** Allow the owner to 'test-drive' the tool ledger without slowing down the shop.

      Keep the UI 'Passive' by default.

      /**

* DATA MODEL: Shop Identity & Vitals
* Update your DataBridge SCHEMA with this:
  \*/
  shop_info: {
  name: 'string',
  is_multi_location: 'boolean', // The "Lite vs Pro" Toggle
  global_contact: {
  phone: 'string',
  email: 'string',
  address: 'string',
  usdot: 'string'
  },
  locations: [
  {
  id: 'string',
  label: 'string',
  phone: 'string',
  email: 'string',
  address: 'string',
  is_primary: 'boolean'
  }
  ]
  }

import React from 'react';
import { Phone, Mail, MapPin, Building2 } from 'lucide-react';
import DataBridge from './DataBridge';

const QuickDialHub = () => {
const shop = DataBridge.load('shop_info') || {
name: "Ignition Shop",
is_multi_location: false,
global_contact: { phone: "512-555-0199", email: "service@ignition.os" }
};

// If single location, we just show one big action card.
// If multi, we map through the locations.
const displayLocations = shop.is_multi_location
? shop.locations
: [{ label: "Main Office", ...shop.global_contact }];

return (

<div className="bg-slate-900 border border-slate-800 rounded-[2.5rem] p-8 shadow-2xl">
<div className="flex items-center gap-3 mb-8">
<Building2 className="text-blue-500" size={20} />
<div>
<h3 className="text-xs font-black text-white uppercase italic tracking-widest">{shop.name}</h3>
<p className="text-[10px] text-slate-500 uppercase font-mono">Support & Dispatch Matrix</p>
</div>
</div>

      <div className="grid gap-4">
        {displayLocations.map((loc, i) => (
          <div key={i} className="bg-slate-950 border border-slate-800 p-5 rounded-3xl flex justify-between items-center transition-all hover:border-blue-500/50">
            <div>
              <p className="text-[10px] font-black text-blue-500 uppercase mb-1">{loc.label}</p>
              <p className="text-sm font-bold text-white uppercase italic">{loc.phone}</p>
            </div>

            <div className="flex gap-3">
              {/* NATIVE PHONE DIALER */}
              <a href={`tel:${loc.phone}`} className="bg-emerald-600 hover:bg-emerald-500 p-4 rounded-2xl text-white shadow-lg shadow-emerald-900/20 transition-all active:scale-90">
                <Phone size={20} fill="white" />
              </a>

              {/* NATIVE MAIL CLIENT */}
              <a href={`mailto:${loc.email}`} className="bg-slate-800 hover:bg-slate-700 p-4 rounded-2xl text-slate-300 border border-slate-700 transition-all active:scale-90">
                <Mail size={20} />
              </a>
            </div>
          </div>
        ))}
      </div>

      {!shop.is_multi_location && (
        <div className="mt-6 pt-6 border-t border-slate-800 flex items-center justify-center gap-2 text-[9px] font-black text-slate-600 uppercase tracking-tighter">
          <MapPin size={12} /> {shop.global_contact.address || "Leander, TX Central"}
        </div>
      )}
    </div>

);
};

export default QuickDialHub;

const LocationToggle = () => {
const [isMulti, setIsMulti] = useState(DataBridge.load('shop_info')?.is_multi_location || false);

const handleToggle = () => {
const current = DataBridge.load('shop_info') || {};
const updated = { ...current, is_multi_location: !isMulti };
DataBridge.save('shop_info', updated);
setIsMulti(!isMulti);
alert(`Shop Mode: ${!isMulti ? 'Multi-Location (Pro)' : 'Single-Location (Lite)'}`);
};

return (
<button
onClick={handleToggle}
className={`px-4 py-2 rounded-lg text-[10px] font-black uppercase transition-all ${isMulti ? 'bg-blue-600 text-white' : 'bg-slate-800 text-slate-500'}`} >
{isMulti ? 'Multi-Location Active' : 'Enable Multi-Location'}
</button>
);
};

      - [ ] **FEATURE: Multi-Role Personal Profile**
      **Logic:** Create `IgnitionProfile.js`.
      - **Tech Interface:** Display tech name, ID, and a 'My Gear' list (pulled from the Hardware Ledger).
      - **Manager Interface:** If `currentUser.permissions` includes 'ADMIN', show the 'Edit Permissions' and 'Labor Rate' settings for the profile being viewed.
      **Identity:** Include a 'Download Identity Badge' button that generates a high-res QR code for the tech's physical toolbox.
      **Privacy:** Ensure that a tech can only view their own profile, while a Manager can browse the entire directory.

      /**

- MODULE: IgnitionProfile
- DESC: User identity, credentials, and tool-custody dashboard.
  \*/

const IgnitionProfile = ({ profileData, isEditable }) => {
return (

<div className="p-6 bg-slate-950 min-h-screen text-slate-200">
{/_ PROFILE HEADER _/}
<div className="flex items-center gap-6 mb-8 bg-slate-900 p-6 rounded-[2.5rem] border border-slate-800 shadow-2xl">
<div className="w-20 h-20 bg-gradient-to-br from-blue-600 to-blue-800 rounded-3xl flex items-center justify-center text-3xl font-black text-white italic shadow-lg">
{profileData.name.charAt(0)}
</div>
<div>
<h2 className="text-2xl font-black text-white uppercase italic tracking-tighter">{profileData.name}</h2>
<p className="text-[10px] font-mono text-blue-500 uppercase tracking-widest">{profileData.id} // {profileData.status}</p>
</div>
</div>

      {/* DYNAMIC PERMISSIONS (Visible to Tech) */}
      <div className="grid grid-cols-1 gap-4 mb-8">
        <div className="bg-slate-900/50 p-4 rounded-2xl border border-slate-800">
          <p className="text-[9px] font-black text-slate-500 uppercase mb-3 tracking-widest text-center">Assigned Access Keys</p>
          <div className="flex flex-wrap gap-2 justify-center">
            {profileData.permissions.map(key => (
              <span key={key} className="bg-blue-600/10 border border-blue-500/30 text-blue-400 text-[9px] font-black px-3 py-1 rounded-full uppercase">
                {key}
              </span>
            ))}
          </div>
        </div>
      </div>

      {/* HARDWARE CUSTODY (The "Where is my stuff?" Section) */}
      <section className="mb-8">
        <h3 className="text-xs font-black uppercase text-slate-400 mb-4 px-2 italic">My Assigned Gear</h3>
        <div className="space-y-2">
          {profileData.assignedTools?.length > 0 ? (
            profileData.assignedTools.map(tool => (
              <div key={tool.id} className="bg-slate-900 p-4 rounded-xl border border-slate-800 flex justify-between items-center">
                <span className="text-xs font-bold text-white uppercase">{tool.name}</span>
                <span className="text-[9px] font-mono text-slate-500 uppercase">SN: {tool.sn}</span>
              </div>
            ))
          ) : (
            <p className="text-[10px] text-slate-600 italic px-2">No shop-owned hardware currently assigned.</p>
          )}
        </div>
      </section>

      {/* THE IDENTITY BADGE (For Shared Kiosks) */}
      <div className="bg-white p-6 rounded-[2rem] text-center shadow-2xl">
        <p className="text-[10px] text-slate-900 font-black uppercase tracking-widest mb-4">Quick-Login Badge</p>
        <div className="bg-slate-100 p-4 rounded-2xl inline-block mb-4">
          {/* QR Component would go here */}
          <div className="w-32 h-32 bg-slate-300 rounded-lg animate-pulse" />
        </div>
        <p className="text-[9px] text-slate-500 uppercase leading-tight">
          Stick this to your toolbox for <br /><strong>Instant Kiosk Access</strong>.
        </p>
      </div>
    </div>

);
};

- [ ] **CORE: Shop Identity & Vitals**
      **Logic:** Update `DataBridge.SCHEMA` to include a `shop_info` object. - **Fields:** `name`, `address`, `phone`, `usdot_number`, `tax_rate`, and `bay_count`.
      **Global Sync:** Replace all hard-coded "Leander Shop" text with a dynamic call to `DataBridge.load('shop_info').name`.
      **Onboarding:** Create a 'Shop Setup' wizard in OMNI for the owner to enter these vitals once during initial deployment.

      - [ ] **FEATURE: Multi-Location Registry & Quick-Dial**
      **Logic:** Update `DataBridge.shop_info` to a `locations` Array.
      - **Fields:** `label`, `phone`, `email`, `is_primary`.
      - **Dialer Logic:** Use `tel:` URI schemes for native mobile dialing.
      **UI (KOSK & HUB):** Create a 'Support Hub' drawer.
      - List all locations with a high-contrast 'CALL' button next to each.
      - **Action:** Tapping 'CALL' triggers the device's native phone dialer with the pre-set number.
      **Email:** Include a 'MAIL' button that opens the native mail client with the location's email pre-filled.

      /**

* COMPONENT: QuickDialHub
* DESC: One-tap communication for mobile techs.
  \*/

const QuickDialHub = () => {
const { locations } = DataBridge.load('shop_info') || { locations: [] };

return (

<div className="bg-slate-900 rounded-[2rem] p-6 border border-slate-800 shadow-2xl">
<h3 className="text-[10px] font-black text-slate-500 uppercase mb-6 tracking-widest text-center">
Location Support Matrix
</h3>
<div className="space-y-3">
{locations.map((loc, i) => (
<div key={i} className="bg-black/40 p-4 rounded-2xl border border-slate-800 flex justify-between items-center">
<div>
<p className="text-xs font-black text-white uppercase italic">{loc.label}</p>
<p className="text-[8px] font-mono text-slate-500">{loc.email}</p>
</div>
<div className="flex gap-2">
{/_ NATIVE PHONE DIALER LINK _/}
<a
href={`tel:${loc.phone}`}
className="bg-emerald-600 p-3 rounded-xl text-white active:scale-90 transition-transform" >
<Phone size={18} fill="white" />
</a>
{/_ NATIVE EMAIL LINK _/}
<a
href={`mailto:${loc.email}`}
className="bg-blue-600 p-3 rounded-xl text-white active:scale-90 transition-transform" >
<Mail size={18} />
</a>
</div>
</div>
))}
</div>
</div>
);
};

- [ ] **UX: Progressive Location Setup**
      **Logic:** Update `OMNI` Setup Wizard to use a toggle: `is_multi_location`. - **IF FALSE (Default):** Display a single form for 'Company Name, Phone, Email, Address'. Save this to `shop_info.global`. - **IF TRUE:** Reveal the 'Location Manager' list. Each entry requires its own contact data.
      **Data Mapping:** - The `QuickDialHub` must check `is_multi_location`. If false, it only shows the 'Main Office' button using global data. - If true, it maps through the `locations` array.

      /**

* COMPONENT: ShopSetupWizard
* DESC: Adapts to the size of the business to minimize friction.
  \*/

const ShopSetupWizard = () => {
const [isMulti, setIsMulti] = useState(false);

return (

<div className="bg-slate-900 p-8 rounded-[2.5rem] border border-slate-800">
<div className="flex justify-between items-center mb-8">
<h3 className="text-sm font-black text-white uppercase italic">Shop Configuration</h3>
<label className="flex items-center gap-2 cursor-pointer">
<span className="text-[10px] font-black text-slate-500 uppercase">Multiple Locations?</span>
<input
type="checkbox"
checked={isMulti}
onChange={(e) => setIsMulti(e.target.checked)}
className="w-4 h-4 rounded border-slate-700 bg-black text-blue-600"
/>
</label>
</div>

      {/* SINGLE LOCATION FORM (LITE) */}
      {!isMulti ? (
        <div className="space-y-4">
          <input placeholder="Company Name" className="w-full bg-black p-4 rounded-xl border border-slate-800 text-white font-bold" />
          <div className="grid grid-cols-2 gap-4">
            <input placeholder="Primary Phone" className="bg-black p-4 rounded-xl border border-slate-800 text-white font-bold" />
            <input placeholder="Primary Email" className="bg-black p-4 rounded-xl border border-slate-800 text-white font-bold" />
          </div>
          <input placeholder="Headquarters Address" className="w-full bg-black p-4 rounded-xl border border-slate-800 text-white font-bold" />
        </div>
      ) : (
        /* MULTI-LOCATION LIST (PRO) */
        <div className="space-y-4">
          <p className="text-[10px] text-blue-500 font-black uppercase">Location Registry</p>
          <div className="p-4 border-2 border-dashed border-slate-800 rounded-2xl text-center text-slate-600 text-[10px] font-black uppercase">
            + Add New Branch (Georgetown, North Austin, etc.)
          </div>
        </div>
      )}
    </div>

);
};

- [ ] **SYNC: Hardware to User Mapping**
      **Logic:** When a tech scans a SHOP tool in KOSK, perform a "Double-Write": 1. Update `Hardware[id].last_assigned_tech = current_user.id`. 2. Update `users_table[current_user.id].assigned_tools` by pushing the tool ID.
      **Goal:** Ensures the 'My Gear' list in the Profile module is always accurate.

- [ ] **ROUTING: Pending Enrollment Guard**
      **Logic:** In `IgnitionCore.js`, add a priority check: - If `current_user.status === 'PENDING_ENROLLMENT'`, force the view to `EnrollmentCatch` even if they have a role assigned. - This prevents techs from bypassing the PIN setup phase.

const MarginEngine = {
calculateRetail: (cost) => {
const numCost = parseFloat(cost);
if (isNaN(numCost)) return 0;

    let markup = 1.25; // Default 25%

    if (numCost <= 50) markup = 4.0;      // 300% Markup
    else if (numCost <= 200) markup = 2.0; // 100% Markup
    else if (numCost <= 1000) markup = 1.5; // 50% Markup

    const retail = numCost * markup;

    // Round to the nearest .99 for that "Professional" look
    return Math.ceil(retail) - 0.01;

},

getProfitMargin: (cost, retail) => {
return ((retail - cost) / retail \* 100).toFixed(2);
}
};

- [ ] **TOGGLE: Relationship Tax & Audit Visibility**
      **Logic:** In `DataBridge.js`, add `shop_policy.enable_price_audit` (Boolean).
      **Control:** In `IgnitionOmni.js` Settings: - Create a toggle switch: "Track Manual Price Deviations." - When toggled OFF: The `LeakageAudit` component in OMNI returns NULL (hidden). - When toggled OFF: The `PriceField` component still allows Admin overrides, but stops recording the `original_calculated_price` metadata to save storage.

      /**

* COMPONENT: AuditToggle
* DESC: Master switch for tracking manual price overrides.
  \*/

const AuditToggle = () => {
const [isActive, setIsActive] = useState(() => {
const policy = DataBridge.load('shop_policy');
return policy?.enable_price_audit ?? false;
});

const toggleAudit = () => {
const currentPolicy = DataBridge.load('shop_policy') || {};
const updated = { ...currentPolicy, enable_price_audit: !isActive };

    DataBridge.save('shop_policy', updated);
    setIsActive(!isActive);

    // Log the policy change for the admin audit trail
    DataBridge.smartSync('POLICY_CHANGE', {
      feature: 'PRICE_AUDIT',
      status: !isActive ? 'ENABLED' : 'DISABLED'
    });

};

return (

<div className="flex items-center justify-between p-4 bg-slate-900 rounded-2xl border border-slate-800">
<div>
<p className="text-xs font-black text-white uppercase italic">Relationship Tax Tracker</p>
<p className="text-[9px] text-slate-500 uppercase">Audit manual price overrides in OMNI reports</p>
</div>
<button
onClick={toggleAudit}
className={`w-12 h-6 rounded-full transition-all relative ${isActive ? 'bg-emerald-600' : 'bg-slate-700'}`} >
<div className={`absolute top-1 w-4 h-4 bg-white rounded-full transition-all ${isActive ? 'left-7' : 'left-1'}`} />
</button>
</div>
);
};

// Inside IgnitionOmni.js
const policy = DataBridge.load('shop_policy');

{policy?.enable_price_audit && (
<LeakageAudit auditData={auditData} />
)}

PHASE 3: FINANCIAL INTEGRITY & MARGIN LOGIC
[ ] FEATURE: Slab-Margin Pricing Engine
Logic: Implement MarginEngine.js logic within DataBridge.

- Consumables ($0-$50): 300% Markup (cost \* 4).
- Mid-Range ($51-$200): 100% Markup (cost \* 2).
- Heavy ($201-$1000): 50% Markup (cost \* 1.5).
- Major ($1001+): 25% Markup (cost \* 1.25).
  Formatting: All results should auto-round to the nearest .99 (e.g., $49.99).

[ ] FEATURE: Admin Price Unlock & Manual Override
Logic: Update Part Entry UI (KOSK/FLUX/STOK).

- Default: Retail price is readOnly based on the Margin Engine.
- Auth Check: If currentUser.permissions includes ADMIN, show a 'Padlock' icon.
- Action: Clicking the Padlock unlocks the field for manual entry.
  Audit Metadata: If a price is manually changed, save original_calculated_price and is_manual_override: true to the job object.

[ ] FEATURE: Relationship Tax Tracker & Master Toggle
Logic: In DataBridge.shop_policy, add enable_price_audit: boolean (Default: FALSE).
UI (OMNI): Add a toggle switch in Settings: "Track Manual Price Deviations."
Reporting: - IF TRUE: Show 'Relationship Tax' column in LeakageAudit comparing Suggested vs. Actual.

- IF FALSE: Hide all manual override alerts and audit columns in OMNI.

[ ] CODE: The PriceField Component

const PriceField = ({ cost, onPriceChange }) => {
const [isLocked, setIsLocked] = useState(true);
const [manualPrice, setManualPrice] = useState(null);
const isAdmin = DataBridge.load('current_user')?.permissions.includes('ADMIN');
const suggestedPrice = MarginEngine.calculateRetail(cost); // Uses Slab Logic
const currentPrice = isLocked ? suggestedPrice : manualPrice;

return (

<div className="flex flex-col gap-2 p-4 bg-slate-900 rounded-2xl border border-slate-800">
<div className="flex justify-between items-center">
<label className="text-[10px] font-black text-slate-500 uppercase tracking-widest">Retail Price</label>
{isAdmin && (
<button onClick={() => setIsLocked(!isLocked)} className={isLocked ? 'text-slate-600' : 'text-orange-500 animate-pulse'}>
{isLocked ? 'Unlock to Discount' : 'Manual Mode Active'}
</button>
)}
</div>
<input
type="number"
value={currentPrice}
readOnly={isLocked}
onChange={(e) => setManualPrice(e.target.value)}
className="bg-transparent text-white font-black text-xl outline-none"
/>
</div>
);
};

- [ ] **FEATURE: SMS Estimate Delivery & E-Sign**
      **Logic:** Update `IgnitionPort.js` to handle digital approval. - **SMS Trigger:** In OMNI, add a 'Send to Customer' button. - **Action:** Uses Twilio API to send a unique, short-link (e.g., `ignition.os/approve/WO-402`).
      **Customer View:** - Opens a mobile-optimized 'Approval Page'. - Displays the line items and the final "Approved Total." - **Signature:** A signature canvas field for the customer to sign.
      **Sync:** Once signed, the job status in OMNI/KOSK moves to 'APPROVED - PROCEED' and saves the signature as a Base64 image in the job log.

- [ ] **CODE: SignatureCanvas Component**

```javascript
import SignatureCanvas from 'react-signature-canvas';

const CustomerApproval = ({ workOrderId, total }) => {
  const sigPad = useRef({});

  const handleSave = () => {
    const signatureData = sigPad.current.toDataURL(); // Capture signature
    DataBridge.save(`approval_${workOrderId}`, {
      signature: signatureData,
      timestamp: new Date().toISOString(),
      status: 'SIGNED'
    });
    alert("Estimate Approved! Shop has been notified.");
  };

  return (
    <div className="p-6 bg-slate-950 min-h-screen flex flex-col items-center">
      <h2 className="text-white font-black italic uppercase mb-4">Approve Work Order</h2>
      <p className="text-blue-500 font-mono text-2xl mb-8">${total}</p>

      <div className="bg-white rounded-3xl p-2 mb-6">
        <SignatureCanvas
          ref={sigPad}
          canvasProps={{width: 300, height: 200, className: 'sigCanvas'}}
        />
      </div>

      <button
        onClick={handleSave}
        className="w-full bg-emerald-600 p-6 rounded-[2rem] text-white font-black uppercase shadow-xl"
      >
        Sign & Authorize Work
      </button>
    </div>
  );
};

###PHASE 3: LOGISTICS
- [ ] **TOGGLE: Passive Asset Custody Switch**
      **Logic:** In `DataBridge.shop_policy`, add `enable_bay_custody: boolean` (Default: FALSE).
      **UI (OMNI Settings):** Add toggle: "Enable Bay-Asset Accountability."
      **Behavior [OFF]:** Techs clock in/out without any mention of shop tools.
      **Behavior [ON]:** Enables the 'Digital Handshake' and 'Safety Valve' window during KOSK clock-in.

- [ ] **CORE: Mandatory PMI Tracker (Non-Optional)**
      **Logic:** Independent of the 'Custody Switch'.
      - All jobs tagged with `type: "INSPECTION"` or `type: "PMI"` must complete the `IgnitionCompliance` checklist before the 'Finish Job' button becomes active.
      - **Audit:** Every completed PMI generates a timestamped log entry: `{wo_id, tech_id, compliance_score, status: "CERTIFIED"}`.


### PHASE 3.5: OPERATIONAL POLISH

- [ ] **FEATURE: Contextual Hand-Off Notes**
      **Logic:** In FLUX, add a 'Shift Change' button.
      - **Action:** Allows the current tech to leave a 140-character "Status Note" that stays pinned to the top of the KOSK for the next tech who clocks in.

- [ ] **UX: The High-Friction Finish**
      **Logic:** Replace the 'Finish Job' button with a 'Slide to Complete' component.
      - **Reason:** Prevents accidental job closures on mobile devices.

- [ ] **CORE: PDF Manifest Generation**
      **Logic:** Integrate a basic 'Print-to-PDF' style for Invoices and DOT forms.
      - **Branding:** Must pull from `shop_info` (Logo, Name, DOT#) to ensure the PDF is legally compliant for fleet records.

      - [ ] **FEATURE: Multi-Day Job Suspension**
      **Logic:** In KOSK, add a 'Suspend' button next to 'Clock Out'.
      - **Suspension Note:** A mandatory text field for the tech to describe the current vehicle state.
      - **Safety Flag:** If the vehicle is "Open" (e.g., oil drained, engine open), the tech can toggle a 'DO NOT MOVE' digital flag.
      **UI (OMNI):** Suspended jobs show a 'Moon' icon. Hovering over the icon shows Tech A’s last note so Tech B knows exactly where to pick up the wrench.
      **Persistence:** Parts already scanned to the job stay 'Reserved' in STOK and cannot be used for other jobs overnight.

      - [ ] **FEATURE: Hands-Free Voice Command (HUB/KOSK)**
      **Logic:** Integrate `WebSpeechAPI` with Audio Focus management.
      - **Wake Word:** Listen for "Ignition..." followed by commands (e.g., "Ignition, Suspend Job", "Ignition, Add 2 Quarts Oil").
      - **Call Handling:** Implement `navigator.mediaDevices` listeners to release the mic immediately on incoming system calls.
      **Hardware Trigger:** Enable 'Proximity Wake'. If the device proximity sensor is covered for 1 second, trigger the 'Listen' state (allows techs to wave a hand over the phone to start talking).
      **UI:** Display a glowing blue 'Audio Aura' around the screen edges when the app is actively listening.

      - [ ] **FEATURE: Power-Sense & Toolbox UI**
      **Logic:** Use the `Battery Status API`.
      - **State [Charging]:** Disable `useSessionGuard` auto-lock; keep screen at 100% brightness; enable 'Always-On' Proximity Wake.
      - **State [Unplugged]:** Re-enable Auto-Lock (5 min); dim screen; require manual tap for voice wake.
      **UI:** Implement 'Toolbox Mode' — Oversized buttons (min 80px) and High-Contrast (Neon/Black) for readability from 3-5 feet.

- [ ] **UX: Haptic Feedback Loop**
      **Logic:** Integrate the `Vibration API`.
      - **Trigger:** On Voice Command success, pulse `[200ms, 100ms, 200ms]`.
      - **Impact:** Provides tactile confirmation through the magnet stand in loud environments.

      ### MARKETING & SALES ASSETS

      - [ ] **ASSET: The 'Leander Pilot' Pitch Deck**
      **Structure:** - Slide 1: The 3 Biggest Leaks (Parts, Tools, Multi-day Labor).
      - Slide 2: The Command Station (Hands-free UX).
      - Slide 3: Regional Pilot Offer (Leander/North Austin special pricing).

- [ ] **ASSET: The 'Voice Command' Cheat Sheet**
      **Logic:** A high-contrast PDF designed to be printed and taped to the side of a tech's toolbox.
      - Includes "Ignition, Clock Out", "Ignition, Suspend", "Ignition, Photo".

- [ ] **LOGIC: The 'Trial-to-Paid' Conversion Hook**
      **Logic:** In OMNI, after 14 days, generate a 'Potential Savings' report.
      - "In 2 weeks, Ignition OS flagged $450 in margin deviations. Unlock the full version to stop the leak."
- [ ] **ARCHITECTURE: Tiered Access Controller**
      **Logic:** Update `shop_policy` to include `tier: "LITE" | "PRO" | "PLATINUM"`.
      **Module Registry:** Create an `available_blocks` array in the DataBridge.
      - **LITE:** Access only to `['CORE', 'KOSK', 'PORT']`.
      - **PRO:** Access to `['CORE', 'KOSK', 'PORT', 'OMNI', 'STOK']` + Paid Add-ons.
      - **PLATINUM:** All indices in `available_blocks` = TRUE.
      **Future-Proofing:** Any new module added to the codebase must check `shop_policy.active_modules.includes(NEW_MODULE_ID)` before mounting.

- [ ] **UI: The 'Ignition Marketplace' (OMNI)**
      **Logic:** In OMNI Settings, show a list of "Locked" modules.
      - **Action:** Clicking a locked module (e.g., 'Predictive Maintenance') triggers a 'Request Activation' ping to you.
Apply DataBridge Schema: Hardware, Locations, and Price Audit keys must be added first.

Mount IgnitionCore: This is the "Bouncer" that manages the views.

Global Strings: Replace "Leander Shop" with DataBridge.load('shop_info').name.

Deploy Passive Features: Ensure the Leaderboard and Tool Tracker are in the code but hidden until toggled in OMNI.

Closing Logic: Ensure the CustomerApproval view is public-facing (doesn't require a tech login) so customers can sign from their own devices via the SMS link.
---

## Archived / Completed

- [x] Integrate Global Labor Economics & `SecurityRegistry` directly into the DataBridge core.
- [x] Lock `CIPHER` estimates point-in-time to securely freeze job prices against admin rate-hikes.
- [x] Hoist `useEffect` to fix the CoreApp React blank-screen login crash.
```
